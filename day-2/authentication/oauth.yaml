apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - htpasswd:
      fileData:
        name: local-users
    mappingMethod: claim
    name: Users
    type: HTPasswd
